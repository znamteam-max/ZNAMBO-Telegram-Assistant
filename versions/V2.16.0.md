# V2.16.0 - Reminder session routing and calendar feedback hardening

V2.16.0 fixes the multi-reminder setup flow so follow-up text like `В 7.00 и 7.30`
creates before-event reminder policies and never edits the event schedule.

Implemented:

```text
multi_reminder_setup_session routes before item_edit_session
policy editor prompts are cleaned up before a new reminder prompt is shown
same-message event plus "напомни за два часа и за полчаса" creates attached policies
reminder-only follow-up messages attach to the latest recent future event
committed plan summaries count policy-materialized reminders and list policy labels
local mutation summaries are sent before calendar best-effort feedback
ended event-like items classify as history before today/important sections
daily history filters standalone fake reminder rows
/admin_repair_v2160 preview|apply adds a conservative local repair path
```

Validation:

```text
npm test -- src/tests/v2160ReminderRouting.test.ts
npx tsc --noEmit
```
