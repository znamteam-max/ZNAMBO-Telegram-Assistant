# V2.5.3 - Production Repair Enforcement and CalDAV Verification

Date: 2026-06-12

V2.5.3 is a focused production hotfix on top of V2.5.2. It does not replace the OpenAI
ActionPlan, scheduler, reconciler, runner lock, or anchor-grid reminder engine.

## Implemented

- Added canonical `buildUserTimelineView` for tasks, dashboard, reminders, and long-term views.
- Added the `unresolved_past` bucket for active work older than 48 hours.
- Strengthened `/admin_repair_v252 preview|apply` and made the repair idempotent.
- Added safe `/cleanup_garbage` preview and `/admin_state_v252`.
- Yandex CalDAV sync now verifies writes with a read-back GET.
- Added safe calendar error classes, `/calendardebug`, and `/calendar_test`.
- Event cards and planner mutation replies now show calendar sync status.

## Pre-Deploy Validation

```text
npm test -> 41 files passed, 129 tests passed
npm run lint -> passed
npx tsc --noEmit -> passed
```

## Production Acceptance

```text
production code commit: febedb885f05b4512dbce25d216b186b64aaf7f4
production URL: https://znambo-telegram-assistant.vercel.app
/api/health: appVersion 2.5.3, Jarvis enabled, runner/reconciler enabled
Telegram webhook: pending 0, no last error
repair preview before apply: 1 orthodontist, 1 Drik duplicate, 5 old overdue records
repair apply: orthodontist fixed, 1 Drik duplicate archived, 4 old records moved to history state
repair preview after apply: all target counts 0
dashboard: compact unresolved-past block with 4 records
reminder smoke: delivered by production scheduler and auto-archived
OpenAI health: real call succeeded with valid structured output
repeated orthodontist phrase: updated canonical orthodontist item, created 0 new items
```

Yandex CalDAV verification returned safe `auth_failed` during authorization. No temporary event was
created. Planner writes remain non-blocking, and `/calendar` now reports the failure explicitly.

Remaining production limitation:

```text
Yandex Calendar needs a valid Yandex app password before create/read/delete can pass.
One future reminder-like item is still reported as an orphan candidate and was intentionally not
archived automatically because it may represent a real user task.
```
