# ZNAMBO Telegram Assistant V2.3.0

Status: released

## Target behavior

- Complete only the item associated with the latest delivered follow-up.
- Represent item mutations as typed `configure`, `complete` and `reschedule` operations.
- Change start/end ranges without duplicate item summaries.
- Preserve active reminder policy when an event is rescheduled.
- Support `show plan + add one item` as a mutation followed by a view.
- Skip existing planner entries before committing a model-proposed ActionPlan.
- Show per-item management buttons on configured follow-up reminders.

## Regression scenarios

```text
Отлично! Выполнено, поставь это сделанным
Дай план на сегодня, добавь вечером подготовку к ЧМ дома
Поменяй подготовку к ЧМ на 22:00, а тренировку на 23:00
Эфир ВС с 13 до 20 сделай
```

## Validation

```text
npm test -> 22 files passed, 75 tests passed
npm run lint -> passed
npm run build -> passed
```

## Production proof

Active behavioral commit:

```text
29cbfa7efcd1e1b8dd47131d32f3696da6d7d01b
```

Verified behavior:

```text
OpenAI remained mandatory and returned valid structured tool calls.
Two-item reschedule targeted exactly the preparation and training item IDs.
Single event range change targeted exactly one active event ID.
Explicit "13 to 20" was stored as 13:00-20:00 Europe/Moscow.
Stale cancelled task-view IDs are rejected by the executor.
Generic completion replies can bind to one fresh follow-up item for 45 minutes.
Hybrid view-plus-create proposals are checked against current active items before commit.
Same-day preparation tasks with the same normalized title are not duplicated.
Configured follow-ups include Done, Reschedule, Edit and Delete buttons.
```

Production data repair:

```text
archived one duplicate "Эфир ВС"
archived one duplicate "Тренировка Z2"
archived one preparation duplicate created during protected verification
preserved the original records with their reminder policies
```

Final active plan:

```text
Эфир ВС -> 13:00-20:00 Europe/Moscow
Подготовка к ЧМ -> 22:00 Europe/Moscow
Тренировка Z2 -> 23:00 Europe/Moscow
```

Operational checks:

```text
health -> ok, appVersion 2.3.0
Telegram webhook -> pending 0, last error none
reminder runner -> ok
```

## Remaining limitations

- The latest-follow-up contextual anchor expires after 45 minutes; later ambiguous replies may require clarification.
- Calendar synchronization remains best-effort and never blocks the assistant database update.
- The protected execution probe does not render Telegram views; the normal webhook pipeline renders the requested post-execution view.
